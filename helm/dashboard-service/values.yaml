replicaCount: 1

image:
  repository: resumescreener-dashboard-service
  pullPolicy: IfNotPresent
  tag: "sha-aac880d"

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

env:

